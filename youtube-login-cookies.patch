diff --git a/desktop/youtubeCookies.js b/desktop/youtubeCookies.js
new file mode 100644
--- /dev/null
+++ b/desktop/youtubeCookies.js
@@ -0,0 +1,129 @@
+"use strict";
+/**
+ * Lets the user log into YouTube inside a real Electron window (its own
+ * persistent session, `persist:youtube-login`) and mirrors that session's
+ * cookies out to cookies.txt in Netscape format - the same file
+ * src/cookies.js already auto-detects for every yt-dlp call.
+ *
+ * Why a *separate* persistent partition instead of the app's default
+ * session: keeps YouTube's auth cookies isolated from anything else the
+ * app's own windows load, and - because Electron persists `persist:*`
+ * session partitions to disk in userData automatically - the user only
+ * has to log in ONCE. Every subsequent app launch just re-syncs
+ * cookies.txt from the already-persisted session (see syncCookiesToFile()
+ * called from main.js's app.whenReady()), no re-login needed until the
+ * session actually expires/gets logged out.
+ */
+const { BrowserWindow, session } = require("electron");
+const fs = require("fs");
+// Reuse the exact same path src/cookies.js auto-detects, so a login here
+// requires zero extra configuration on the yt-dlp side.
+const { DEFAULT_COOKIES_FILE } = require("../src/cookies");
+
+const PARTITION = "persist:youtube-login";
+const LOGIN_URL = "https://accounts.google.com/ServiceLogin?service=youtube&continue=https%3A%2F%2Fwww.youtube.com%2F";
+
+// Fallback expiry for session cookies (no expirationDate from Chromium) -
+// yt-dlp's Netscape parser wants a real epoch, not "0 = end of session".
+const FALLBACK_EXPIRY_DAYS = 180;
+
+function toNetscapeLine(cookie) {
+  const domain = cookie.domain || "";
+  // Netscape's 2nd column means "applies to subdomains too" - true when the
+  // cookie has a leading-dot domain (which is how Chromium reports
+  // non-host-only cookies), false for exact-host-only cookies.
+  const includeSubdomains = domain.startsWith(".") ? "TRUE" : "FALSE";
+  const path = cookie.path || "/";
+  const secure = cookie.secure ? "TRUE" : "FALSE";
+  const expiry =
+    typeof cookie.expirationDate === "number"
+      ? Math.round(cookie.expirationDate)
+      : Math.floor(Date.now() / 1000) + FALLBACK_EXPIRY_DAYS * 86400;
+  return [domain, includeSubdomains, path, secure, expiry, cookie.name, cookie.value].join("\t");
+}
+
+/**
+ * Pulls every cookie relevant to YouTube out of the login partition and
+ * writes them to <BASE_DIR>/cookies.txt. Safe to call even if the user has
+ * never logged in (just writes an empty-but-valid cookies file, which
+ * src/cookies.js will pick up as "no useful cookies" - yt-dlp treats an
+ * empty Netscape file the same as no cookies at all).
+ */
+async function syncCookiesToFile() {
+  const ses = session.fromPartition(PARTITION);
+
+  // get({url}) returns exactly what Chromium would send for a request to
+  // that URL (domain-matching included); querying both hosts covers the
+  // youtube.com session cookies AND the underlying google.com auth cookies
+  // (SID/HSID/SAPISID/etc.) some yt-dlp code paths also check for.
+  const [ytCookies, googleCookies] = await Promise.all([
+    ses.cookies.get({ url: "https://www.youtube.com" }),
+    ses.cookies.get({ url: "https://www.google.com" }),
+  ]);
+
+  const seen = new Set();
+  const merged = [];
+  for (const c of [...ytCookies, ...googleCookies]) {
+    const key = `${c.domain}|${c.path}|${c.name}`;
+    if (seen.has(key)) continue;
+    seen.add(key);
+    merged.push(c);
+  }
+
+  const lines = [
+    "# Netscape HTTP Cookie File",
+    "# Auto-generated by KanaeDesktop (desktop/youtubeCookies.js) - do not edit by hand.",
+    ...merged.map(toNetscapeLine),
+    "",
+  ];
+
+  fs.writeFileSync(DEFAULT_COOKIES_FILE, lines.join("\n"), "utf-8");
+
+  console.log(`[YouTube Login] cookies.txt disinkron (${merged.length} cookie).`);
+  return merged.length;
+}
+
+/**
+ * Opens (or focuses, if already open) the YouTube login window. Syncs
+ * cookies.txt as soon as the window is closed, regardless of whether the
+ * login actually succeeded (a no-op sync if nothing changed).
+ */
+let _loginWin = null;
+
+function openLoginWindow(parentWin, onStatus) {
+  if (_loginWin && !_loginWin.isDestroyed()) {
+    _loginWin.show();
+    _loginWin.focus();
+    return;
+  }
+
+  _loginWin = new BrowserWindow({
+    width: 480,
+    height: 680,
+    parent: parentWin || undefined,
+    modal: false,
+    title: "Login ke YouTube",
+    webPreferences: {
+      partition: PARTITION,
+      contextIsolation: true,
+      nodeIntegration: false,
+      sandbox: true,
+    },
+  });
+
+  _loginWin.setMenuBarVisibility(false);
+  _loginWin.loadURL(LOGIN_URL);
+
+  _loginWin.on("closed", async () => {
+    _loginWin = null;
+    try {
+      const count = await syncCookiesToFile();
+      if (onStatus) onStatus({ ok: true, count });
+    } catch (e) {
+      console.error(`[YouTube Login] Gagal sinkron cookies: ${e.message}`);
+      if (onStatus) onStatus({ ok: false, error: e.message });
+    }
+  });
+}
+
+module.exports = { openLoginWindow, syncCookiesToFile, PARTITION };
diff --git a/desktop/main.js b/desktop/main.js
--- a/desktop/main.js
+++ b/desktop/main.js
@@ -16,6 +16,7 @@
 const { spawn } = require("child_process");
 const path = require("path");
 const fs = require("fs");
+const youtubeCookies = require("./youtubeCookies");
 
 // Chromium's Wayland/Vulkan GPU path (color-management, image-transfer-function
 // warnings, "not compatible with Vulkan" for --ozone-platform=wayland) is still
@@ -188,6 +189,19 @@
 ipcMain.handle("win:get-api-base", () => API_BASE);
 ipcMain.on("win:open", (event, name) => openChildWindow(name));
 
+// ── YouTube login (cookies) IPC ─────────────────────────────────
+// Opens a real login window; cookies.txt gets (re)synced automatically
+// as soon as it's closed. Status is pushed back to whichever window asked,
+// so e.g. settings.html can show "Tersimpan (42 cookie)" / an error.
+ipcMain.on("youtube:login", (event) => {
+  const requester = BrowserWindow.fromWebContents(event.sender);
+  youtubeCookies.openLoginWindow(win, (status) => {
+    if (requester && !requester.isDestroyed()) {
+      requester.webContents.send("youtube:login-status", status);
+    }
+  });
+});
+
 app.whenReady().then(async () => {
   startBackend();
   try {
@@ -197,6 +211,14 @@
   }
   createWindow();
 
+  // Silently re-sync cookies.txt from whatever was persisted in the
+  // youtube-login session partition on a PREVIOUS run - Electron keeps
+  // persist:* partitions on disk, so this means the user only ever has to
+  // click "Login ke YouTube" once, not on every app launch.
+  youtubeCookies.syncCookiesToFile().catch((e) => {
+    console.warn(`[YouTube Login] Sync awal gagal (mungkin belum pernah login): ${e.message}`);
+  });
+
   app.on("activate", () => {
     if (BrowserWindow.getAllWindows().length === 0) createWindow();
   });
diff --git a/desktop/preload.js b/desktop/preload.js
--- a/desktop/preload.js
+++ b/desktop/preload.js
@@ -9,5 +9,10 @@
   // Opens (or focuses, if already open) a secondary window.
   // name: "settings" | "tiktok" | "search"
   openWindow: (name) => ipcRenderer.send("win:open", name),
+  // Opens the "login into YouTube" window; cookies.txt is auto-synced when
+  // it's closed. onLoginStatus fires once after that with { ok, count } or
+  // { ok: false, error }.
+  loginYoutube: () => ipcRenderer.send("youtube:login"),
+  onYoutubeLoginStatus: (cb) => ipcRenderer.on("youtube:login-status", (_event, status) => cb(status)),
   platform: process.platform,
 });
diff --git a/desktop/renderer/settings.html b/desktop/renderer/settings.html
--- a/desktop/renderer/settings.html
+++ b/desktop/renderer/settings.html
@@ -144,6 +144,17 @@
       </div>
 
       <div class="cfg-section">
+        <div class="cfg-section-title">Login YouTube (Cookies)</div>
+        <p style="font-size:11px;color:var(--text-dim);line-height:1.6;margin:0 0 8px">
+          Login sekali di sini supaya yt-dlp pakai cookies akun kamu — biasanya jauh lebih jarang kena 403 dibanding tanpa login. Cookies disimpan otomatis (Netscape cookies.txt) dan dipakai lagi setiap kali app dibuka, tidak perlu login ulang tiap kali.
+        </p>
+        <div class="cfg-row" style="gap:8px;flex-wrap:wrap">
+          <button class="btn btn-success btn-sm" id="btnYoutubeLogin">Login ke YouTube</button>
+          <span id="youtubeLoginStatus" style="font-size:11px;color:var(--text-dim)"></span>
+        </div>
+      </div>
+
+      <div class="cfg-section">
         <div class="cfg-section-title">Command Keywords</div>
         <div class="cfg-row"><span class="cfg-label">Request lagu<small>mis. #req #lagu</small></span><div class="tag-input-wrap" id="tags-cmd-request"></div></div>
         <div class="cfg-row"><span class="cfg-label">Skip / next<small>mis. #skip #next</small></span><div class="tag-input-wrap" id="tags-cmd-skip"></div></div>
@@ -291,6 +302,21 @@
   $("btn-min").onclick = () => window.desktop && window.desktop.minimize();
   $("btn-close").onclick = () => window.desktop && window.desktop.close();
 
+  if (window.desktop && window.desktop.loginYoutube) {
+    $("btnYoutubeLogin").onclick = () => {
+      $("youtubeLoginStatus").textContent = "Membuka jendela login...";
+      window.desktop.loginYoutube();
+    };
+    window.desktop.onYoutubeLoginStatus((status) => {
+      $("youtubeLoginStatus").textContent = status.ok
+        ? `Tersimpan (${status.count} cookie) - ${new Date().toLocaleTimeString()}`
+        : `Gagal: ${status.error}`;
+    });
+  } else {
+    $("btnYoutubeLogin").disabled = true;
+    $("youtubeLoginStatus").textContent = "Hanya tersedia di desktop app (Electron).";
+  }
+
   // ── Tabs ─────────────────────────────────
   document.querySelectorAll("#tabs button").forEach((btn) => {
     btn.onclick = () => {
